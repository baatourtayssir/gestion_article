<form method="post" href="{{ path('app_article_delete', {'id': article.id}) }}" onclick="return confirm('Are you sure you want to delete this item?');">

	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
	<button class="btn">Supprimer</button>

</form>


{# onsubmit #}
