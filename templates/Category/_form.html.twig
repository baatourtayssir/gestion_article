{# {{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-success">{{ button_label|default('Sauvegarder') }}</button>

<br>
{{ form_end(form) }} #}


{% extends 'base.html.twig' %}

{% block title %}Nouvelle catégorie

{% endblock %}

{% block body %}
	<h1>Créer une nouvelle catégorie</h1>


	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-success">{{ button_label|default('Sauvegarder') }}</button>

	<br>
	{{ form_end(form) }}

	<br>
	<a href="{{ path('app_category_index') }}" class="btn btn-dark">Retour à la liste</a>

{% endblock %}
